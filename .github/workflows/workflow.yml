name: CI

on: [push]

jobs:
  build:

    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v2
    - run: echo "Hello world 🗺"


    - uses: ./node

    - uses: ./python
    
    - uses: joshmgross/hello-go@latest
      with:
        name: "Person"
        phrase: "hello"
